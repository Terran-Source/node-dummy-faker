enum DataType {
  int = 'number',
  number = 'number',
  bigInt = 'bigInt',
  bigNumber = 'bigInt',
  float = 'double',
  double = 'double',
  date = 'date',
  datetime = 'date',
  json = 'json',
  object = 'json',
  bool = 'boolean',
  boolean = 'boolean',
  hex = 'hex',
  uuid = 'uuid',
  guid = 'uuid',
  string = 'string',
  default = 'string',
}

export default DataType;
